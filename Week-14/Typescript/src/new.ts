// "target": "es2016",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
//     "rootDir": "./src",                                   
//     "outDir": "./dist",


// in tsconfig.json
/**we have stored all the .ts files in src and converted ,js files in dist folder 
 * such that we can add dist flder to git ignore and only .ts files will be pushed to github
 */