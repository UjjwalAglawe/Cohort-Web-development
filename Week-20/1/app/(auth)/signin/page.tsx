export default function Signin()
{
    //to reach thi sroute we not have to go to auth/signin
    //but we have to go to /signin    () folder is ignored

    //layout .tsx  all the children will be wrapped inside 
    // used for headers and footer
    return(
        <div className="container">
            Signin
        </div>
    )
}